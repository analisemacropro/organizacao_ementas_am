# Planilha de controle interno (preços, links de compra, nº de alunos) — não publicar
Organizacao_Cursos_AM.csv

# Cursos fora da planilha (descontinuados)
fora-da-planilha/

# Material-fonte dos workshops (notebooks, ebooks, dados, .env com chaves) —
# não publicar; o repo só leva as ementas renderizadas em 07-...
Workshops Ativos/

# Caches do Quarto
.quarto/
*_cache/

# Dados de clientes — NUNCA versionar (export de pedidos do WooCommerce)
orders-*.csv

# Python (dashboard)
__pycache__/
*.pyc
.venv/
venv/
env/

# Sistema
.DS_Store
Thumbs.db

# Backup do .git corrompido (recuperação local, não versionar)
.git_CORROMPIDO_BACKUP/

# Exports gerados pelo dashboard
inscritos_analise_macro_*.csv
inscritos_analise_macro_*.xlsx

# Planilha consolidada para o Drive (datada) — controle interno
Organizacao_Cursos_AM_*.xlsx

# Credenciais da API do Google Sheets (script de atualização do Drive) — NUNCA versionar
scripts/credentials.json
scripts/token.json
